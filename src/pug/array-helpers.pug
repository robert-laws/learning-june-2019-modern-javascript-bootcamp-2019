extends layouts/default.pug

block content
  div(class="container")
    h4 Array Helpers

    p forEach loop

    div(class="code")
      code let names = ["bob", "hal", "mel", "ned", "abe", "jim", "lou", "kal", "vin", "don", "sal"];
      code(class="space") 
      code names.forEach(function(name) {
      code(class="indent") console.log(name);
      code })

    div(id="result-one" class="example")

    div(class="code")
      code let numbers = [1, 2, 3, 4, 5, 6];
      code let sum = 0;
      code(class="space") 
      code function add(number) {
      code(class="indent") sum += number;
      code }
      code(class="space")
      code number.forEach(add);

    div(id="result-two" class="example")

